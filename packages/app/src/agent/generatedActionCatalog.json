[
  {
    "actionType": "sampleView/addMetadata",
    "description": "Add imported sample metadata to the current collection.",
    "payloadType": "SetMetadata",
    "payloadDescription": "Add imported sample metadata to the current collection.",
    "payloadFields": [
      {
        "name": "columnarMetadata",
        "type": "ColumnarMetadata",
        "description": "Metadata encoded in columnar form, aligned to the sample order.",
        "required": true
      },
      {
        "name": "attributeDefs",
        "type": "Record<AttributeName, SampleAttributeDef>",
        "description": "Optional attribute definitions for the incoming metadata columns.",
        "required": false
      },
      {
        "name": "replace",
        "type": "boolean",
        "description": "If true, the provided metadata will replace existing metadata instead of being added to them.",
        "required": false
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "sampleView/deriveMetadata",
    "description": "Add derived metadata computed from a view-backed attribute.",
    "payloadType": "DeriveMetadata",
    "payloadDescription": "Add derived metadata computed from a view-backed attribute.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      },
      {
        "name": "name",
        "type": "string",
        "description": "Name of the derived metadata column. The resulting metadata is written under this name.",
        "required": true
      },
      {
        "name": "groupPath",
        "type": "string",
        "description": "Optional metadata group path for the derived column.",
        "required": false
      },
      {
        "name": "scale",
        "type": "SampleAttributeDef[\"scale\"]",
        "description": "Optional scale definition for the derived metadata column.",
        "required": false
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "sampleView/addMetadataFromSource",
    "description": "Add metadata imported from an external source.",
    "payloadType": "AddMetadataFromSource",
    "payloadDescription": "Add metadata imported from an external source.",
    "payloadFields": [
      {
        "name": "sourceId",
        "type": "string",
        "description": "Optional source identifier. If omitted, source resolution is allowed only when exactly one source exists.",
        "required": false
      },
      {
        "name": "columnIds",
        "type": "string[]",
        "description": "Columns requested from the source.",
        "required": true
      },
      {
        "name": "groupPath",
        "type": "string",
        "description": "Optional metadata group path override.",
        "required": false
      },
      {
        "name": "replace",
        "type": "boolean",
        "description": "If true, replace existing metadata instead of merging.",
        "required": false
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "sampleView/sortBy",
    "description": "Sort samples in descending order by the chosen attribute.",
    "payloadType": "SortBy",
    "payloadDescription": "Payload for sorting samples by an attribute.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      }
    ],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "age"
      }
    }
  },
  {
    "actionType": "sampleView/retainFirstOfEach",
    "description": "Retain the first sample of each category in the current ordering.",
    "payloadType": "RetainFirstOfEach",
    "payloadDescription": "Payload for retaining the first sample of each category.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "sampleView/retainFirstNCategories",
    "description": "Retain samples from the first n categories in the current ordering.",
    "payloadType": "RetainFirstNCategories",
    "payloadDescription": "Retain samples from the first n categories in the current ordering.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      },
      {
        "name": "n",
        "type": "number",
        "description": "Number of categories to retain.",
        "required": true
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "sampleView/filterByQuantitative",
    "description": "Retain or remove samples using a numeric comparison.",
    "payloadType": "FilterByQuantitative",
    "payloadDescription": "Payload for filtering samples by comparing a quantitative attribute against a numeric operand.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      },
      {
        "name": "operator",
        "type": "ComparisonOperatorType",
        "description": "Comparison operator used for thresholding.",
        "required": true
      },
      {
        "name": "operand",
        "type": "number",
        "description": "Numeric threshold used in the comparison.",
        "required": true
      }
    ],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "purity"
      },
      "operator": "gte",
      "operand": 0.6
    }
  },
  {
    "actionType": "sampleView/filterByNominal",
    "description": "Retain or remove samples by exact discrete values.",
    "payloadType": "FilterByNominal",
    "payloadDescription": "Retain or remove samples by exact discrete values.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      },
      {
        "name": "values",
        "type": "any[]",
        "description": "Discrete values to match.",
        "required": true
      },
      {
        "name": "remove",
        "type": "boolean",
        "description": "If true, matching samples are removed instead of retained.",
        "required": false
      }
    ],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "diagnosis"
      },
      "values": ["AML"]
    }
  },
  {
    "actionType": "sampleView/removeUndefined",
    "description": "Remove samples that are missing the chosen attribute.",
    "payloadType": "RemoveUndefined",
    "payloadDescription": "Payload for removing samples missing an attribute value.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "sampleView/groupCustomCategories",
    "description": "Create custom sample groups from manually chosen categories.",
    "payloadType": "GroupCustom",
    "payloadDescription": "Create custom sample groups from manually chosen categories.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      },
      {
        "name": "groups",
        "type": "CustomGroups",
        "description": "A record where the keys are group names and the values are arrays of categories or sample ids.",
        "required": true
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "sampleView/groupByNominal",
    "description": "Group samples by the distinct values of the chosen attribute.",
    "payloadType": "GroupByNominal",
    "payloadDescription": "Payload for grouping by a categorical attribute.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "sampleView/groupToQuartiles",
    "description": "Group samples into quartiles by the chosen quantitative attribute.",
    "payloadType": "GroupToQuartiles",
    "payloadDescription": "Payload for grouping by quartiles.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "sampleView/groupByThresholds",
    "description": "Group samples by user-defined numeric thresholds.",
    "payloadType": "GroupByThresholds",
    "payloadDescription": "Group samples by user-defined numeric thresholds.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute identifier used by the action.",
        "required": true
      },
      {
        "name": "thresholds",
        "type": "[Threshold, ...Threshold[]]",
        "description": "Thresholds used to stratify the samples.",
        "required": true
      }
    ],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "purity"
      },
      "thresholds": [
        {
          "operator": "lte",
          "operand": 0.2
        },
        {
          "operator": "lt",
          "operand": 0.8
        }
      ]
    }
  },
  {
    "actionType": "sampleView/removeGroup",
    "description": "Remove a nested sample group by path.",
    "payloadType": "RemoveGroup",
    "payloadDescription": "Remove a nested sample group by path.",
    "payloadFields": [
      {
        "name": "path",
        "type": "string[]",
        "description": "An array of group names that represent the path to the group. The implicit ROOT group is excluded.",
        "required": true
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "sampleView/retainMatched",
    "description": "Retain categories that are present in all current groups.",
    "payloadType": "RetainMatched",
    "payloadDescription": "Retain categories that are present in all current groups.",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "description": "Attribute whose categories must be present in all current groups.",
        "required": true
      }
    ],
    "examplePayload": {}
  },
  {
    "actionType": "paramProvenance/paramChange",
    "description": "Update a bookmarkable parameter.",
    "payloadType": "ParamProvenanceEntry",
    "payloadDescription": "Payload for updating a bookmarkable parameter entry in provenance.",
    "payloadFields": [
      {
        "name": "selector",
        "type": "ParamSelector",
        "description": "Structured selector for the parameter.",
        "required": true
      },
      {
        "name": "value",
        "type": "ParamValue",
        "description": "Serialized parameter value.",
        "required": true
      },
      {
        "name": "origin",
        "type": "ParamOrigin",
        "description": "Optional datum origin for replayable values.",
        "required": false
      }
    ],
    "examplePayload": {
      "selector": {
        "scope": [],
        "param": "brush"
      },
      "value": {
        "type": "interval",
        "intervals": {
          "x": [
            {
              "chrom": "chr17",
              "pos": 7685012
            },
            {
              "chrom": "chr17",
              "pos": 7690727
            }
          ]
        }
      }
    }
  }
]
