/**
 * Built-in theme that preserves the current GenomeSpy visuals.
 *
 * This is intentionally empty because INTERNAL_DEFAULT_CONFIG already encodes
 * the current behavior. The layer is kept explicit so additional built-ins can
 * be added without changing the merge model.
 *
 * @type {import("../spec/config.js").GenomeSpyConfig}
 */
export const CURRENT_BEHAVIOR_THEME = {};
