[
  {
    "actionType": "sampleView/deriveMetadata",
    "description": "Add a derived metadata column by copying sample attributes or aggregating features from a genomic interval.",
    "usage": "Use this action to derive one per-sample value aggregated from features in an active interval selection. If the user names a gene, locus, or interval and no matching selection aggregation candidate exists yet, proactively create or update the interval selection using the `paramChange` action and wait for a refreshed context, which reveals selection aggregation candidates. Then use the matching `SELECTION_AGGREGATION` candidate from `selectionAggregation.fields`. To aggregate only specific features, e.g., deleterious variants, use `AttributeIdentifier.featureFilter`. This action does not import from a source.",
    "payloadType": "DeriveMetadata",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      },
      {
        "name": "name",
        "type": "string",
        "typeRefs": [],
        "description": "Name of the derived metadata column. This becomes the leaf attribute name written into metadata. Prefer a short user-facing name.",
        "required": true
      },
      {
        "name": "groupPath",
        "type": "string",
        "typeRefs": [],
        "description": "Optional metadata group path for the derived column. When provided, the resulting attribute is written under `groupPath/name`.",
        "required": false
      },
      {
        "name": "scale",
        "type": "SampleAttributeDef[\"scale\"] | null",
        "typeRefs": ["Scale"],
        "description": "If omitted, derived metadata may inherit an authored source scale when the aggregation preserves the source value domain. Use `null` to force automatic scale inference without inheritance.",
        "required": false
      }
    ],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "purity"
      },
      "name": "purity_copy"
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "purity"
        },
        "name": "purity_copy"
      },
      {
        "attribute": {
          "type": "SELECTION_AGGREGATION",
          "candidateId": "genomeSelection@variants:VAF",
          "aggregation": "count",
          "featureFilter": {
            "field": "CADD_phred",
            "operator": "gt",
            "value": 28.5
          }
        },
        "name": "highCADDVariants"
      }
    ]
  },
  {
    "actionType": "sampleView/addMetadataFromSource",
    "description": "Import (missing) metadata columns from a metadata source.",
    "usage": "Use this to dynamically add new metadata columns from a configured source.",
    "payloadType": "AddMetadataFromSource",
    "payloadFields": [
      {
        "name": "sourceId",
        "type": "string",
        "typeRefs": [],
        "description": "Configured metadata source to read from. Omit this only when exactly one source is available.",
        "required": false
      },
      {
        "name": "columnIds",
        "type": "string[]",
        "typeRefs": [],
        "description": "Column ids to import from the selected source.",
        "required": true
      },
      {
        "name": "groupPath",
        "type": "string",
        "typeRefs": [],
        "description": "Optional metadata group path override for the imported columns.",
        "required": false
      },
      {
        "name": "replace",
        "type": "boolean",
        "typeRefs": [],
        "description": "Whether to replace the current metadata set. If `true`, only the imported columns remain. If omitted or `false`, the imported columns are merged into the existing metadata.",
        "required": false
      }
    ],
    "examplePayload": {
      "columnIds": ["diagnosis", "stage", "MYC"]
    },
    "examples": [
      {
        "columnIds": ["diagnosis", "stage", "MYC"]
      }
    ]
  },
  {
    "actionType": "sampleView/sortBy",
    "description": "Sort samples by a selected attribute.",
    "usage": "Use this when samples should be ranked by one quantitative or ordinal attribute before further filtering or grouping. The attribute may be metadata or a selection-derived aggregation candidate from `selectionAggregation.fields`. Sorting is stable, so ties preserve current sample order. The default order is descending.",
    "payloadType": "SortBy",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      },
      {
        "name": "order",
        "type": "\"ascending\" | \"descending\"",
        "typeRefs": [],
        "description": "Sort direction. __Default value:__ `\"descending\"`",
        "required": false
      }
    ],
    "attributeKinds": ["quantitative", "ordinal"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "age"
      },
      "order": "descending"
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "age"
        },
        "order": "descending"
      }
    ]
  },
  {
    "actionType": "sampleView/retainFirstOfEach",
    "description": "Retain representative samples by keeping the first sample for each distinct value of a selected attribute.",
    "usage": "Use this to reduce to one sample per category value, for example one sample per patient. Workflow: first sort by ranking attribute(s) if desired, then use this action to keep the top-ranked sample for each category value, e.g., patient or subjectId.",
    "payloadType": "RetainFirstOfEach",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      }
    ],
    "attributeKinds": ["nominal", "ordinal"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "patient_id"
      }
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "patient_id"
        }
      }
    ]
  },
  {
    "actionType": "sampleView/retainFirstNCategories",
    "description": "Retain categories by first occurrence in the current sample order; do not use for top-k category sizes.",
    "usage": "Iterate over samples in current order, pick first n distinct values, and filter by those values. Use this after first ordering samples by a ranking attribute, for example to keep all samples from the first n patients after sorting by mutational burden. Do not use category order from `getAttributeSummary`; it is not the current sample order. For top-k groups by size, use `retainGroupsByRank`. For group-size thresholds, use `retainGroupsBySize`.",
    "payloadType": "RetainFirstNCategories",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      },
      {
        "name": "n",
        "type": "number",
        "typeRefs": [],
        "description": "Maximum number of distinct categories to retain. Categories are taken in first-seen order within each current group.",
        "required": true
      }
    ],
    "attributeKinds": ["nominal", "ordinal"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "patient_id"
      },
      "n": 5
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "patient_id"
        },
        "n": 5
      }
    ]
  },
  {
    "actionType": "sampleView/filterByQuantitative",
    "description": "Retain samples whose selected quantitative value satisfies a threshold comparison.",
    "usage": "Use this for numeric filters such as values greater than, less than, or equal to a chosen threshold. The attribute may be metadata or a selection-derived aggregation candidate from `selectionAggregation.fields`. Only matching samples remain. To keep all samples from category values that match a condition on another attribute, use `retainCategoriesByAttribute`.",
    "payloadType": "FilterByQuantitative",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      },
      {
        "name": "operator",
        "type": "ComparisonOperatorType",
        "typeRefs": ["ComparisonOperatorType"],
        "description": "Comparison applied as `attributeValue operator operand`.",
        "required": true
      },
      {
        "name": "operand",
        "type": "number",
        "typeRefs": [],
        "description": "Numeric value on the right-hand side of the comparison.",
        "required": true
      }
    ],
    "attributeKinds": ["quantitative"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "purity"
      },
      "operator": "gte",
      "operand": 0.6
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "purity"
        },
        "operator": "gte",
        "operand": 0.6
      }
    ]
  },
  {
    "actionType": "sampleView/retainCategoriesByAttribute",
    "description": "Retain all samples from category values that have matching samples according to a condition on another attribute.",
    "usage": "Use this when a categorical attribute links related samples, such as multiple samples from the same patient, and the whole category should be retained if related samples match a quantitative or categorical condition. This is like `WHERE patient_id IN (SELECT patient_id FROM samples WHERE TP53_mutation_count > 0)`. If the condition depends on filtering raw features inside a selection, first use `deriveMetadata` to create that filtered per-sample attribute. Then use this action only if all samples from matching category values should be retained. Quantitative conditions compare the condition attribute to a numeric operand. Categorical conditions use `operator: \"in\"` with `values`. With categorical conditions, `required: \"any\"` keeps categories with at least one selected condition value and `required: \"all\"` requires every selected condition value to occur in the category.",
    "payloadType": "RetainCategoriesByAttribute",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Categorical attribute whose categories are retained. All samples with a retained category value are kept.",
        "required": true
      },
      {
        "name": "condition",
        "type": "AttributeCondition",
        "typeRefs": ["AttributeCondition"],
        "description": "Per-sample condition used to decide which values of `attribute` are retained. A value of `attribute` is retained when at least one sample with that value satisfies this condition. All samples with retained `attribute` values are kept.",
        "required": true
      }
    ],
    "attributeKinds": ["nominal", "ordinal"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "patient_id"
      },
      "condition": {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "TP53_mutation_count"
        },
        "operator": "gt",
        "operand": 0
      }
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "patient_id"
        },
        "condition": {
          "attribute": {
            "type": "SAMPLE_ATTRIBUTE",
            "specifier": "TP53_mutation_count"
          },
          "operator": "gt",
          "operand": 0
        }
      },
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "patient_id"
        },
        "condition": {
          "attribute": {
            "type": "SAMPLE_ATTRIBUTE",
            "specifier": "diagnosis"
          },
          "operator": "in",
          "values": ["AML", "MDS"],
          "required": "all"
        }
      }
    ]
  },
  {
    "actionType": "sampleView/filterByNominal",
    "description": "Retain or remove samples whose selected attribute matches any of the provided values.",
    "usage": "Use this for exact-match filtering on categorical or ordinal attributes. Set `remove` to `true` to exclude matching samples instead of keeping them. Only matching samples remain. To keep all samples from category values that match a condition on another attribute, use `retainCategoriesByAttribute`.",
    "payloadType": "FilterByNominal",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      },
      {
        "name": "values",
        "type": "NominalFilterValue[]",
        "typeRefs": ["NominalFilterValue"],
        "description": "Attribute values matched by exact equality.",
        "required": true
      },
      {
        "name": "remove",
        "type": "boolean",
        "typeRefs": [],
        "description": "Whether to remove matching samples instead of retaining them. If omitted or `false`, only matching samples are kept.",
        "required": false
      }
    ],
    "attributeKinds": ["nominal", "ordinal"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "diagnosis"
      },
      "values": ["AML"]
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "diagnosis"
        },
        "values": ["AML"]
      }
    ]
  },
  {
    "actionType": "sampleView/removeUndefined",
    "description": "Remove samples whose selected attribute value is missing.",
    "usage": "Use this before later analysis steps when samples with `undefined` or `null` values should be excluded. The attribute may be metadata or a selection-derived aggregation candidate from `selectionAggregation.fields`.",
    "payloadType": "RemoveUndefined",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      }
    ],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "purity"
      }
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "purity"
        }
      }
    ]
  },
  {
    "actionType": "sampleView/groupCustomCategories",
    "description": "Add a grouping level to the sample hierarchy using named groups and explicit mapping of attribute values.",
    "usage": "Use this when specific attribute values should be merged into custom named groups. Samples whose value is not listed in `groups` are not kept in the grouped result.",
    "payloadType": "GroupCustom",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      },
      {
        "name": "groups",
        "type": "CustomGroups",
        "typeRefs": ["CustomGroups"],
        "description": "Mapping from output group name to attribute values assigned to that group. Samples whose value is not listed in any group are omitted from the grouped result.",
        "required": true
      }
    ],
    "attributeKinds": ["nominal", "ordinal"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "diagnosis"
      },
      "groups": {
        "Myeloid": ["AML", "MDS"],
        "Lymphoid": ["ALL", "CLL"]
      }
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "diagnosis"
        },
        "groups": {
          "Myeloid": ["AML", "MDS"],
          "Lymphoid": ["ALL", "CLL"]
        }
      }
    ]
  },
  {
    "actionType": "sampleView/groupByNominal",
    "description": "Add a grouping level to the sample hierarchy using a categorical attribute.",
    "usage": "Creates one group per visible category. Group order follows the resolved attribute domain, and categories with no samples are omitted.",
    "payloadType": "GroupByNominal",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      }
    ],
    "attributeKinds": ["nominal", "ordinal"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "diagnosis"
      }
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "diagnosis"
        }
      }
    ]
  },
  {
    "actionType": "sampleView/groupToQuartiles",
    "description": "Add a grouping level to the sample hierarchy using quartile-based bins of a quantitative attribute.",
    "usage": "Use this for a quick quantitative stratification. Quartiles are computed from the current samples in each group using the R-7 method, and tied values may collapse adjacent quartiles. The attribute may be metadata or a selection-derived aggregation candidate from `selectionAggregation.fields`.",
    "payloadType": "GroupToQuartiles",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      }
    ],
    "attributeKinds": ["quantitative"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "purity"
      }
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "purity"
        }
      }
    ]
  },
  {
    "actionType": "sampleView/groupByThresholds",
    "description": "Add a grouping level to the sample hierarchy using threshold-defined numeric intervals of a quantitative attribute.",
    "usage": "Use this when quantitative bins should follow explicit thresholds instead of quartiles. The attribute may be metadata or a selection-derived aggregation candidate from `selectionAggregation.fields`. The number of resulting groups is thresholds + 1.",
    "payloadType": "GroupByThresholds",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute operated on by the action. Its current values are resolved before the reducer runs and then used by sorting, filtering, grouping, or metadata derivation.",
        "required": true
      },
      {
        "name": "thresholds",
        "type": "[Threshold, ...Threshold[]]",
        "typeRefs": ["Threshold"],
        "description": "Thresholds used to stratify the samples. Supply these in ascending operand order. Adjacent thresholds define the output intervals.",
        "required": true
      },
      {
        "name": "groupTitles",
        "type": "string[]",
        "typeRefs": [],
        "description": "Optional display titles for the generated groups. When provided, the array must contain one title for each output interval: exactly `thresholds.length + 1` titles. Titles are ordered from the lowest interval to the highest interval, matching the ascending threshold order. For thresholds `[10, 20]`, titles correspond to values below `10`, values from `10` to `20`, and values above `20`. Titles must be non-empty and unique. The original interval labels are still preserved for each group.",
        "required": false
      }
    ],
    "attributeKinds": ["quantitative"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "purity"
      },
      "thresholds": [
        {
          "operator": "lte",
          "operand": 0.2
        },
        {
          "operator": "lt",
          "operand": 0.8
        }
      ]
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "purity"
        },
        "thresholds": [
          {
            "operator": "lte",
            "operand": 0.2
          },
          {
            "operator": "lt",
            "operand": 0.8
          }
        ]
      }
    ]
  },
  {
    "actionType": "sampleView/removeGroup",
    "description": "Remove a previously created sample group by path.",
    "usage": "Use this to delete one visible group from the current grouping hierarchy. The path is relative to the implicit root group. This action cannot remove grouping levels; jump back in provenance instead.",
    "payloadType": "RemoveGroup",
    "payloadFields": [
      {
        "name": "path",
        "type": "string[]",
        "typeRefs": [],
        "description": "Group names from outermost to innermost, excluding the implicit ROOT.",
        "required": true
      }
    ],
    "examplePayload": {
      "path": ["Group 1"]
    },
    "examples": [
      {
        "path": ["Group 1"]
      }
    ]
  },
  {
    "actionType": "sampleView/retainGroupsByRank",
    "description": "Retain top-k or bottom-k groups by size at a grouping level.",
    "usage": "Use this for requests such as \"keep the five most abundant groups\", \"retain the top 5 cancer types\", or \"keep the 10 smallest groups\". Set `limit` to k. Set `order: \"descending\"` for top-k, highest values, largest groups by size, or most abundant groups. Set `order: \"ascending\"` for bottom-k, lowest values, smallest groups by size, or least abundant groups. Ranked retention is applied separately within each ancestor partition. For example, with `level: 2` and `limit: 3`, each level-1 group keeps its own three largest or smallest child groups. Use `level: 1` for top-level groups under ROOT. Only `measure: \"size\"` is currently supported. Size means the number of descendant visible samples in the group. Ties preserve current group order.",
    "payloadType": "RetainGroupsByRank",
    "payloadFields": [
      {
        "name": "level",
        "type": "number",
        "typeRefs": [],
        "description": "One-based grouping level to filter. `1` targets top-level groups under ROOT. `2` targets direct children of level-1 groups, and so on. For top-level categories such as cancer type, use `1`.",
        "required": true
      },
      {
        "name": "measure",
        "type": "\"size\"",
        "typeRefs": [],
        "description": "Group-level measure used for ranking. Only `\"size\"` is supported. Size means group abundance: the number of descendant visible samples in the group.",
        "required": true
      },
      {
        "name": "limit",
        "type": "number",
        "typeRefs": [],
        "description": "Number of ranked groups to retain within each ancestor partition. This is k in top-k or bottom-k requests. For example, use `5` to keep the five largest or five smallest groups.",
        "required": true
      },
      {
        "name": "order",
        "type": "\"descending\" | \"ascending\"",
        "typeRefs": [],
        "description": "Ranking direction. Use `\"descending\"` for top-k, highest values, largest groups by size, or most abundant groups. Use `\"ascending\"` for bottom-k, lowest values, smallest groups by size, or least abundant groups.",
        "required": true
      }
    ],
    "examplePayload": {
      "level": 1,
      "measure": "size",
      "limit": 5,
      "order": "descending"
    },
    "examples": [
      {
        "level": 1,
        "measure": "size",
        "limit": 5,
        "order": "descending"
      },
      {
        "level": 2,
        "measure": "size",
        "limit": 3,
        "order": "descending"
      }
    ]
  },
  {
    "actionType": "sampleView/retainGroupsBySize",
    "description": "Retain groups at a grouping level by size threshold.",
    "usage": "Use this for threshold requests such as \"retain groups with at least 10 samples\". This is different from top-k retention: it keeps every group at the selected level whose size satisfies `operator` and `operand`, regardless of rank. Use `level: 1` for top-level groups under ROOT. Only `measure: \"size\"` is currently supported. Size means the number of descendant visible samples in the group.",
    "payloadType": "RetainGroupsBySize",
    "payloadFields": [
      {
        "name": "level",
        "type": "number",
        "typeRefs": [],
        "description": "One-based grouping level to filter. `1` targets top-level groups under ROOT. `2` targets direct children of level-1 groups, and so on. For top-level categories such as cancer type, use `1`.",
        "required": true
      },
      {
        "name": "measure",
        "type": "\"size\"",
        "typeRefs": [],
        "description": "Group-level measure used for thresholding. Only `\"size\"` is supported. Size means group abundance: the number of descendant visible samples in the group.",
        "required": true
      },
      {
        "name": "operator",
        "type": "ComparisonOperatorType",
        "typeRefs": ["ComparisonOperatorType"],
        "description": "Threshold comparison applied as `measure operator operand`.",
        "required": true
      },
      {
        "name": "operand",
        "type": "number",
        "typeRefs": [],
        "description": "Numeric group-size threshold.",
        "required": true
      }
    ],
    "examplePayload": {
      "level": 1,
      "measure": "size",
      "operator": "gte",
      "operand": 10
    },
    "examples": [
      {
        "level": 1,
        "measure": "size",
        "operator": "gte",
        "operand": 10
      }
    ]
  },
  {
    "actionType": "sampleView/ungroup",
    "description": "Collapse a grouping level and all deeper levels.",
    "usage": "Use this after group-retention actions when the user wants to keep the retained samples but remove grouping structure. This keeps only samples currently present in the hierarchy; it does not restore samples removed by previous actions. If the user only wants to reverse a previous grouping action and restore the prior state, use provenance/undo instead. Use `level: 1` to collapse top-level groups under ROOT into one flat sample list. Use `level: 2` to keep level-1 groups but collapse their children and all deeper levels into sample lists.",
    "payloadType": "Ungroup",
    "payloadFields": [
      {
        "name": "level",
        "type": "number",
        "typeRefs": [],
        "description": "One-based grouping level to collapse. `1` collapses top-level groups under ROOT and all deeper levels into one flat ROOT sample group. `2` keeps level-1 groups but collapses their children and all deeper levels into sample lists.",
        "required": true
      }
    ],
    "examplePayload": {
      "level": 1
    },
    "examples": [
      {
        "level": 1
      }
    ]
  },
  {
    "actionType": "sampleView/retainMatched",
    "description": "Retain samples whose selected value appears in every current non-empty group.",
    "usage": "Use this for intersection-style cohort refinement after samples have already been split into groups. A value is kept only if it appears in every current non-empty group.",
    "payloadType": "RetainMatched",
    "payloadFields": [
      {
        "name": "attribute",
        "type": "AttributeIdentifier",
        "typeRefs": ["AttributeIdentifier"],
        "description": "Attribute whose values must be present in every current non-empty group.",
        "required": true
      }
    ],
    "attributeKinds": ["nominal", "ordinal"],
    "examplePayload": {
      "attribute": {
        "type": "SAMPLE_ATTRIBUTE",
        "specifier": "patient_id"
      }
    },
    "examples": [
      {
        "attribute": {
          "type": "SAMPLE_ATTRIBUTE",
          "specifier": "patient_id"
        }
      }
    ]
  },
  {
    "actionType": "paramProvenance/paramChange",
    "description": "Create or update a reactive selection or parameter value, including point selections, interval selections, and genomic-region selections.",
    "usage": "Use this for point selections, interval selections, genomic-region selections, and other replayable parameter changes. If you create a temporary selection for an ephemeral purpose (e.g. to compute a one-off metadata column), clear it out with a subsequent `paramChange` that sets the interval to null.",
    "payloadType": "ParamProvenanceEntry",
    "payloadFields": [
      {
        "name": "selector",
        "type": "ParamSelector",
        "typeRefs": ["ParamSelector"],
        "description": "Structured selector for the parameter.",
        "required": true
      },
      {
        "name": "value",
        "type": "ParamValue",
        "typeRefs": ["ParamValue"],
        "description": "Serialized parameter value.",
        "required": true
      },
      {
        "name": "origin",
        "type": "ParamOrigin",
        "typeRefs": ["ParamOrigin"],
        "description": "Optional datum origin for replayable values.",
        "required": false
      }
    ],
    "examplePayload": {
      "selector": {
        "scope": [],
        "param": "sel-brush"
      },
      "value": {
        "type": "interval",
        "intervals": {
          "x": [
            {
              "chrom": "chr17",
              "pos": 7685012
            },
            {
              "chrom": "chr17",
              "pos": 7690727
            }
          ]
        }
      }
    },
    "examples": [
      {
        "selector": {
          "scope": [],
          "param": "sel-brush"
        },
        "value": {
          "type": "interval",
          "intervals": {
            "x": [
              {
                "chrom": "chr17",
                "pos": 7685012
              },
              {
                "chrom": "chr17",
                "pos": 7690727
              }
            ]
          }
        }
      },
      {
        "selector": {
          "scope": [],
          "param": "sel-brush"
        },
        "value": {
          "type": "interval",
          "intervals": {}
        }
      },
      {
        "selector": {
          "scope": [],
          "param": "semanticZoomSlider"
        },
        "value": {
          "type": "value",
          "value": 0
        }
      }
    ]
  }
]
