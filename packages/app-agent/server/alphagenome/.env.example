# Copy to .env and fill in values.
# .env is gitignored — never commit real tokens.

# Port the server listens on.
AG_PORT=8002

# HuggingFace token for downloading model weights (required on first run).
HF_TOKEN=hf_...
