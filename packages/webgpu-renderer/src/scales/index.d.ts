import type { ConfiguredScale, ScaleDef, ScaleOptions } from "../index.js";

export const indexScaleDefinition: ScaleDef;
export function indexScale(options?: ScaleOptions): ConfiguredScale<"index">;
