import type { ConfiguredScale, ScaleDef, ScaleOptions } from "../index.js";

export const symlogScaleDefinition: ScaleDef;
export function symlogScale(options?: ScaleOptions): ConfiguredScale<"symlog">;
