import type { ConfiguredScale, ScaleDef, ScaleOptions } from "../index.js";

export const ordinalScaleDefinition: ScaleDef;
export function ordinalScale(
    options?: ScaleOptions
): ConfiguredScale<"ordinal">;
