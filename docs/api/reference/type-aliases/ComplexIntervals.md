[GenomeSpy Core API](../index.md) / ComplexIntervals

# Type Alias: ComplexIntervals

> **ComplexIntervals** = `Partial`<`Record`<`"x"` \| `"y"`, [`ComplexInterval`](ComplexInterval.md) \| `null`\>\>

Complex interval values keyed by positional channel, or `null` when clear.
