# Sample

The `"sample"` transform takes a random sample of the data objects.

## Parameters

SCHEMA SampleParams

## Example

```json
{
  ...
  "transform": [
    { "type": "sample", "size": 100 }
  ],
  ...
}
```
