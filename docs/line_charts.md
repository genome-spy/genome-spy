# Line Charts

## Introduction

Line charts are a type of chart used to display data as a series of connected points. In GenomeSpy, line charts can be used to display QTL mapping and allele effect data.

## Usage

To display a line chart, simply call the `display_qtl_mapping` or `display_allele_effect` method on the corresponding view.

## Example