{
  "$schema": "https://unpkg.com/@genome-spy/core/dist/schema.json",

  "description": [
    "Zoomable brushing and linking with hierarchical params.",
    "The top view pushes interval selection to an outer param.",
    "The bottom view links x domain to the same param on a zoomable scale.",
    "Brushing changes the linked domain, and zoom/pan in the linked view updates the brush."
  ],

  "params": [{ "name": "brush" }],

  "resolve": {
    "scale": { "x": "independent" }
  },

  "data": {
    "sequence": { "start": 0, "stop": 101, "step": 1, "as": "x" }
  },

  "transform": [{ "type": "formula", "expr": "sin(datum.x / 7)", "as": "y" }],

  "vconcat": [
    {
      "height": 100,
      "params": [
        {
          "name": "brush",
          "select": { "type": "interval", "encodings": ["x"] },
          "push": "outer"
        }
      ],
      "mark": { "type": "point", "size": 20, "opacity": 0.35 },
      "encoding": {
        "x": { "field": "x", "type": "quantitative" },
        "y": { "field": "y", "type": "quantitative" }
      }
    },
    {
      "height": 120,
      "mark": { "type": "point", "size": 55, "opacity": 0.75 },
      "encoding": {
        "x": {
          "field": "x",
          "type": "quantitative",
          "scale": {
            "zoom": true,
            "domain": { "param": "brush" }
          }
        },
        "y": { "field": "y", "type": "quantitative" }
      }
    }
  ]
}
