/*
import Model, { createModel } from "./model.js";

test("Fails on empty spec", () => {
    expect(createModel({})).toThrow();
});

test("Parses trivial spec", () => {
    const spec = {
        mark: "point"
    };
    
    expect(createModel(spec)).toBeTruthy();
});
*/